<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Edit comment form</h1>
    <div>ID: <%= comment.id %></div>
    <div>Username: <%= comment.username %></div>

    <!-- send as a regular POST request, Node will replace "?_method" with PATCH -->
    <form method="POST" action="/comments/<%= comment.id %>?_method=PATCH">
        <textarea name="comment" id="comment" cols="30" rows="5"><%= comment.comment %></textarea>
      <br />
      <button type="submit">Save</button>
    </form>

    <a href="/comments">Cancel</a>
  </body>
</html>
