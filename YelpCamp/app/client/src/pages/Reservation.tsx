import React from 'react';

const Reservation = () => {
    return (
        <div>
            Confirm Reservation
            <button>Pay</button>
        </div>
    );
};

export default Reservation;
